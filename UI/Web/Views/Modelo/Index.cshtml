@model IEnumerable<SistemaMAV.UI.Web.Models.ModeloViewModel>

@{
    ViewData["Title"] = "Modelos";
    string objName = this.ViewContext.RouteData.Values["controller"].ToString();
}

@Html.Raw(SistemaMAV.UI.Web.Helpers.References.GetReferences_DataTables())
    
<div class="row">
    <div class="col-xs-12">
        <div class="box box-primary">
            <div class="box-header">
                <h3 class="box-title">@ViewBag.Title</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
                <p>
                    <a class="btn btn-primary btn-sm" href="/@Html.Raw(objName)/Create" title="Nuevo"><span class="glyphicon glyphicon-plus" style="margin-right: 5px;"></span>Nuevo</a>
                </p>
                <table id="grd" class="table table-bordered table-striped table-hover">
                    <thead>
                        <tr>
                            <th></th>
                            <th>
                                @Html.DisplayNameFor(model => model.Detalle)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Marca)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.TipoUnidad)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.FechaAlta)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.FechaBaja)
                            </th>
                        </tr>
                    </thead>
                    <tbody>
@foreach (var item in Model) {
                        <tr>
                            <td>
                                <a class="btn btn-primary btn-xs" href="/@Html.Raw(objName)/Edit/@Html.Raw(item.ModeloId)" title="Modificar"><span class="glyphicon glyphicon-pencil"> </span></a>
                                <a class="btn btn-default btn-xs" href="/@Html.Raw(objName)/Details/@Html.Raw(item.ModeloId)" title="Detalles"><span class="glyphicon glyphicon-eye-open"> </span></a>
                                <a class="btn btn-danger btn-xs" href="/@Html.Raw(objName)/Delete/@Html.Raw(item.ModeloId)" title="Borrar"><span class="glyphicon glyphicon-remove"> </span></a>
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Detalle)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Marca.Detalle)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TipoUnidad.Detalle)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.FechaAlta)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.FechaBaja)
                            </td>
                        </tr>
}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<!-- page script -->
<script>
    $(document).ready(function () {
        initDataGrid("grd");
    });
</script>
